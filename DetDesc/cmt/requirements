# =============================================================================
package           DetDesc
version           v18r27p1

# =========== structure =======================================================
branches      DetDesc doc cmt src

# =========== dependencies ====================================================
use           GaudiKernel       *
use           GaudiUtils        *
use           ROOT              * LCG_Interfaces

# =========== own includes ====================================================
apply_pattern install_more_includes more=DetDesc

# =========== constituents ====================================================
library       DetDescLib        Lib/*.cpp -no_static

# =========== standard patterns (the order is essential!) =====================
apply_pattern linker_library    library=DetDescLib

# =============== LCG Dictionaries ============================================
apply_pattern     reflex_dictionary                                 \
                  dictionary=DetDesc                                \
                  headerfiles=$(DETDESCROOT)/dict/DetDescDict.h     \
                  selectionfile=$(DETDESCROOT)/dict/DetDescDict.xml \
                  options="-U__MINGW32__"

macro_append ROOT_linkopts " -lHist -lMathCore -lGenVector " \
             WIN32 " libHist.lib libMathCore.lib libGenVector.lib "

